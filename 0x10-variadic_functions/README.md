# C - Variadic functions

In this 17th lesson of C Programming language, we explore variadic functions

Variadic Functions are functions which may take variable number of arguments and are declared with ellipsis (...) in place of the last parameter. An example of such function is printf. These functions must have at least one named parameter.
